import { EditorSettings } from '@/model/EditorSettings';

export const DEFAULT_BACKGROUND_COLOR = 'rgb(25, 25, 25)';

export const DEFAULT_SETTINGS: EditorSettings = {
  backgroundColor: DEFAULT_BACKGROUND_COLOR,
};
